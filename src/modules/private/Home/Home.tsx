import AddButton from '@/components/AddButton/AddButton';
import { ResponsiveBox } from '@/components/ResponsiveBox';

const FutesScreen = () => {
  return (
    <ResponsiveBox>
      <AddButton to="/outra-pagina" />
    </ResponsiveBox>
  );
};

export default FutesScreen;

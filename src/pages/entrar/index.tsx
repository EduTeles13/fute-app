import { PageLayout } from '@/components';
import { LoginPage } from '@/modules/LoginPage';

const Login = () => {
  return (
    <PageLayout>
      <LoginPage />
    </PageLayout>
  );
};

export default Login;
